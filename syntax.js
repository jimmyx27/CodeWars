function main(verb, noun) {
  return verb + noun;
}

function sayHello(name) {
  return `Hello ${name}`;
}

var a = "dev";
var b = "lab";
var name = a + b;

function addFive(num) {
  var total = num + 5;
  return total;
}
